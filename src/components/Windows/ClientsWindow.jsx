import React from 'react';
import Icon from '../UI/Icon';

const ClientsWindow = ({ subModule, data }) => {
  return (
    <div className="p-6 h-full overflow-auto bg-gray-50">
      {/* ... contenido del componente ... */}
    </div>
  );
};

export default ClientsWindow;